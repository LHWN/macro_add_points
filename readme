

while True:
    if check_quiz_isAvailabe
        quiz_button 클릭
            ex: 버튼 못 찾으면 뒤로가기 버튼
        
        quiz_name = "" 사이 글자만 뽑기

        if quiz_name == "" 글자 없으면 (성형 이벤트 아니거나 초성 퀴즈)
            뒤로 가기 버튼 누르기 or 네트워크 오류 확인 버튼 
            1번이면 2번으로, 2번이면 1번으로 지정
            continue
        
        정답 입력하기 클릭
            ex: 이미 푼거면 뒤로가기 하고 1번은 2번으로, 2번은 1번으로, 네트워크 오류 알림창 있으면 확인 누르고 뒤로가기 클릭 continue
        
        키워드 복사 버튼 클릭
        다시 캐시닥 앱으로 이동
        

        if 리스트에 없는 퀴즈면: 직접 크롬으로 찾기
        else: 리스트에서 가져오기

        답 입력하고 제출하고
            ex: 오류나면 예외처리
        
        x 버튼 클릭하기
        뒤로가기 클릭
    else: 목록에서 이름에 "이벤트" 없으면 뒤로 갔다가 다시 들어오기



## 처리해야 할 예외 케이스들
- (완료) 두 개 다 이미 푼거면 back_refresh
- 로하셀 처리


    
